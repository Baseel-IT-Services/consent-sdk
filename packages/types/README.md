# @baseel/types

Shared TypeScript types used across the Baseel Consent SDK
(`@baseel/consent-web-component`, `@baseel/consent-react`, `@baseel/loader`).

```bash
npm install @baseel/types
```

You generally don't need to install this directly — it's a dependency of
the other Baseel SDK packages. Install it explicitly only if you're writing
your own integration against the SDK's types.

Part of the [Baseel SDK monorepo](https://github.com/baseel-sdk/baseel-sdk).

## License

[MIT](../../LICENSE) © Baseel
